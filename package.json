{
  "name": "blast-radius",
  "private": true,
  "version": "0.1.0",
  "description": "Blast Radius — an erasure agent that rehearses deletions before it commits them.",
  "engines": { "node": ">=22.14" },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
